# DEV-LOG-001: CODE_WIKI 生成 → 学习回路诊断 → doc-contract 方案双轮审计 → ADR-0006 双份权威源

> **日期**: 2026-08-16
> **会话**: Claude GLM-5.3（主控站）
> **涉及**: CODE_WIKI.md / spec/doc-contract/PLAN.md / adr/ADR-0006 / 仓库 git 化
> **状态**: 本日志记录时点，ADR-0006 仍为 proposed（方案 B 待用户确认）

---

## 做了什么（时序）

1. **CODE_WIKI.md 生成**：全仓库 9 份存量文档逐份分析，产出结构化 Wiki（四层架构/10 步流程/取证矩阵/依赖图/悬空引用清单）。头部误写"8 个文档"——后续自查以逐项枚举纠正为 9（1+2+1+1+4），统计溯源规则的自我实例。
2. **学习回路诊断**：识别框架"事故触发回路闭合、数据触发回路开环"——4 项已具备 / 5 项缺失（规则效果无量化、无退出机制、失效条件无触发器、规则不可机读、迁移教训无回流）。最小升级路径三条：```rules``` 登记块 / 命中率日志 / 规则 re-qualification。
3. **doc-contract 改造方案**（现 PLAN.md v1.3）：G1-G4 全局动作 + 25 处逐文档修改清单。三轮版本：v1.0 初版 → v1.1 P1 修正 → v1.2 P2 修正 → v1.3 ADR-0006 涟漪。
4. **双轮审计**（详见决策依据①）：同基座自查 10 项发现 → 异构复审 12 项发现 → E1 亲验。
5. **ADR-0006 落盘**：双份 ASSERTION_EVIDENCE_FRAMEWORK 差异分析 + 权威源决策（方案 B 提案）。
6. **仓库 git 化**：init + 首推 GitHub（远程 LICENSE 提交 rebase 整合，320617d）。

## 决策依据

### ① 审计流程选择：全按 SPEC_PROCESS v1.3 规则执行

- **规则 1（时序独立）**：review 在方案完稿后独立 pass 执行
- **规则 4（单视角声明）**：自查结论全标 `自查（单视角）`
- **规则 5（异构复审）**：复审 agent 为 DeepSeek V4 Pro（双盲：仅喂方案文本+证据，未见推理过程），合规升级为 `[已复核·异构]`。用户后续更正复审基座信息，触发状态重定性
- **规则 6（证据绑定）**：复审 7 项 P2 全部经 E1 亲验后融入——并抓出**复审自身漏计 1 处**（DIS-007 L68 的 assertion_audit.py 引用，Grep 实测 3 处 L4/L68/L86，复审只报 L86）

### ② M7 数据点（N=2，ADR-0007 附录正式承载）

| 审计轮 | 基座 | 发现 | 特征 |
|--------|------|------|------|
| 自查 | GLM 5.3（既写又审） | 10 项（2P1+5P2+3P3） | 笔误级：行号/计数/断链数 |
| 异构复审 | DeepSeek V4 Pro（双盲） | 12 项（7P2+5P3） | 系统性遗漏：涟漪/档位越界/目标缺失 |
| 交叉 | P2 重叠率 1/12；异构自身漏计 1 处被 E1 亲验抓出 | 无单一审查视角充分 | |

### ③ DIS-001~006 存在性：探针推翻原假设

原方案"DIS-001~006 断档登记"是无据推断（从文件名 007 反推）。探针（Glob/Grep）实证：**全部存在于 Cpp_Hub/docs/discoveries/**（001_bh_fdr ~ 006_fp_determinism 完整枚举）。登记改"拆分迁移"。教训：框架自己的 existence probe 案例在自家方案上复活，B 类断言必须过探针。

### ④ P0 撞名处理：撤销改名，消歧代替

原拟"P0 审计项→K 级"改名。复核涟漪：术语在 ADR-0005（不可变记录）与 CODE_WIKI 4 处出现，改名即制造新不一致。改道：ADR-0007 术语节加消歧条目，存量原文不动。

### ⑤ 双份框架权威源：方案 B（提案）

fc.exe 比对输出 GBK 乱码，行号与 Read 实测不符（乱码示 355+ 行，实测 304 行）——**乱码不采信，清洁取证一律 Read**。差异实证：本仓库份 v1.0（258 行）vs Cpp_Hub 份 v1.1（304 行），同日漂移一代，且 007 文件成对漂移（Cpp_Hub 份有版本行，本仓库份无）。ADR-0006 提案本仓库为权威（v1.2 回吸收增量 + Cpp_Hub 加指针 + 回流通道）。

### ⑥ 编号让位

ADR-0006 被双份权威源决策占用 → doc-contract 文档规范让位 ADR-0007（PLAN.md 全局替换已完成）。

## 遇到的问题

| 问题 | 处置 |
|------|------|
| fc.exe GBK 乱码，行号不可信 | 取证教训登记入 ADR-0006 §验证；Windows 取证用 Read/git diff |
| PowerShell 不支持 heredoc commit | 改多 `-m` 参数（环境事实，非决策） |
| 初次 push 被拒（远程已有 LICENSE） | fetch → `pull --rebase --allow-unrelated-histories` → push 成功 |
| SPEC_PROCESS 约定路径（docs/adr/、docs/dev-log/）与本仓库物理布局（adr/）不一致 | 本日志按物理路径落 docs/dev-log/；ADR 索引落 docs/adr/README.md 作编号登记；路径真值统一留给 doc-contract Step B（S2/S6），不提前双轨 |

## 下一步

1. **[待用户]** ADR-0006 方案 B 确认 → 执行 v1.2 增量合并（≈50 行）+ Cpp_Hub 侧指针
2. doc-contract Step A-G 执行（G3/G4 标注起步，零风险）
3. P3 遗留 5 项随 ADR-0007 定稿处理
4. M7 对比臂以本会话为第 3 个样本（同基座 vs 异构 vs E1 亲验三层）
