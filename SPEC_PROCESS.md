# Spec 驱动开发规范

---
id: SPEC-PROCESS
type: process-spec
version: 1.4
status: active
date: 2026-08-17
depends: [ADR-0004, ADR-0005, ADR-0008, ADR-0009, FWK-ASSERTION]
upstream: null
---

> **建立日期**：2026-08-01
> **修订**：v1.1（2026-08-01，M6 元审计后增补 Review 独立性规则）→ v1.2（2026-08-01，外部对标后：规则 3 升级为隔离四要素、新增规则 5 异质性约束、Step 8 增派生需求项）→ **v1.2.1**（2026-08-16，自包含化：全部教训内联正文，移除对 ADR/元审计报告/本地 skill 路径的内容依赖，可跨项目迁移复用）→ **v1.3**（2026-08-16，新增规则 6 审计证据绑定：Step 10 审计报告取证矩阵标准化——证据五分类 E1-E5、双向映射、诚实结果列、最高等级绑定、风险分级执行）→ **v1.4**（2026-08-17，cpp-hub-absorption Tier2：Step 2 Review 升格门禁语义 D4 + Step 8 双向引用/断言延续两项 D6（[ADR-0008](adr/ADR-0008-spec-process-review-gate-and-bidirectional-check.md)）+ Step 2/10 发现记录集成点 D5（[ADR-0009](adr/ADR-0009-discoveries-log-mechanism.md)））
> **适用范围**：math-finance-reasoning 项目的所有功能开发（**迁移说明**：本文档自 v1.2.1 起自包含，所有教训、规则依据、ADD 原则均已内联；其他项目可直接复制使用，正文中的 M6/M2 等历史案例为本项目实测教训，迁移时可替换为新项目自身案例，规则本身不变）
> **命名空间说明（DC4）**：文中 M2/M6 等为源项目元审计里程碑编号（M1-M7），全局登记见 [ADR-0007 附录 A](adr/ADR-0007-unified-document-contract.md)；M7 审计轮证据账本：[docs/M7_EVIDENCE_LOG.md](docs/M7_EVIDENCE_LOG.md)
> **核心原则**：调研先行 → 文档驱动 → 多轮 Review 排除幻觉 → TDD 实现 → 审查验收

---

## 10 步 Spec 流程

```
┌─────────────────────────────────────────────────────────────┐
│  Phase 1: 调研                                              │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ Step 1: 使用多种 MCP 工具调研                         │    │
│  │   - mcp_paper-search / mcp_english-search            │    │
│  │   - mcp_research-tools / mcp_scholar-mirror          │    │
│  │   - WebSearch / WebFetch                             │    │
│  │   - SearchCodebase（如涉及现有代码）                  │    │
│  └──────────────────────┬──────────────────────────────┘    │
│                         ↓                                    │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ Step 2: Review 形成调研文档                           │    │
│  │   - 使用 research 模板                                │    │
│  │   - 排除幻觉：验证所有文献引用（arXiv 编号、作者、年份）│    │
│  │   - 标注置信度                                        │    │
│  │   - 输出: spec/<feature>/RESEARCH.md             │    │
│  └──────────────────────┬──────────────────────────────┘    │
└─────────────────────────┼───────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│  Phase 2: 设计                                              │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ Step 3: 基于调研文档编写设计文档                       │    │
│  │   - 使用 design 模板                                  │    │
│  │   - 架构选择、模块划分、接口定义、数据流               │    │
│  │   - 输出: spec/<feature>/DESIGN.md                │    │
│  └──────────────────────┬──────────────────────────────┘    │
│                         ↓                                    │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ Step 4: Review 排除幻觉                               │    │
│  │   - 检查设计是否基于已验证的调研结论                    │    │
│  │   - 检查是否有未经验证的假设                           │    │
│  │   - 检查是否有论证驱动的归因扭曲                       │    │
│  └──────────────────────┬──────────────────────────────┘    │
└─────────────────────────┼───────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│  Phase 3: 实施                                              │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ Step 5: 基于调研文档编写实施文档                       │    │
│  │   - 使用 implementation 模板                          │    │
│  │   - 工程细节：版本、依赖、兼容性、接口签名              │    │
│  │   - 排除低效操作                                      │    │
│  │   - 输出: spec/<feature>/IMPLEMENTATION.md        │    │
│  └──────────────────────┬──────────────────────────────┘    │
│                         ↓                                    │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ Step 6: Review 排除幻觉                               │    │
│  │   - 检查实施细节是否与设计对齐                         │    │
│  │   - 检查依赖版本是否真实存在                           │    │
│  │   - 检查接口签名是否可实现                             │    │
│  └──────────────────────┬──────────────────────────────┘    │
└─────────────────────────┼───────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│  Phase 4: 验收                                              │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ Step 7: 编写审查验收 checklist                        │    │
│  │   - 使用 checklist 模板                               │    │
│  │   - 每个验收项可测试、有明确通过条件                    │    │
│  │   - 输出: spec/<feature>/CHECKLIST.md             │    │
│  └──────────────────────┬──────────────────────────────┘    │
│                         ↓                                    │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ Step 8: Review 文档之间的一致性                        │    │
│  │   - RESEARCH.md ↔ DESIGN.md 对齐检查                  │    │
│  │   - DESIGN.md ↔ IMPLEMENTATION.md 对齐检查            │    │
│  │   - IMPLEMENTATION.md ↔ CHECKLIST.md 对齐检查         │    │
│  │   - 识别并修复对齐问题                                 │    │
│  └──────────────────────┬──────────────────────────────┘    │
└─────────────────────────┼───────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│  Phase 5: 实现                                              │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ Step 9: TDD 实现                                      │    │
│  │   - 先写测试（基于 CHECKLIST.md）                      │    │
│  │   - 再写实现                                          │    │
│  │   - 测试通过 ≠ 设计落地（遵循 ADD Iron Law）           │    │
│  └──────────────────────┬──────────────────────────────┘    │
│                         ↓                                    │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ Step 10: 审查验收                                     │    │
│  │   - 运行 CHECKLIST.md 的所有验收项                     │    │
│  │   - 执行 ADD（audit-driven-development）              │    │
│  │   - 产出取证矩阵（规则 6）                             │    │
│  │   - 记录开发日志                                      │    │
│  │   - 更新 PROGRESS.md                                  │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

## 文档目录结构

每个功能（feature）在 `spec/` 下有独立目录：

```
spec/<feature>/
├── RESEARCH.md          # Step 2 产出：调研文档
├── DESIGN.md            # Step 3 产出：设计文档
├── IMPLEMENTATION.md    # Step 5 产出：实施文档
└── CHECKLIST.md         # Step 7 产出：验收 checklist
```

## Review 检查清单

### Review 独立性规则（v1.1 新增，M6 元审计教训）

> **背景**：M6 首次执行中，Step 4/6 的 review checkbox 与正文在同一次 Write 中
> 打勾（"并发自查"），Step 8 一致性表在实现前预填；事后元审计证实该模式有真实漏网
> （`Decimal.ulp` 为 3.12+ API，Step 6"版本已验证"声明失实导致 22 个测试失败；
> §10.1 验收统计从 pytest 总数推算，4 项错误处理零测试被虚报为 7/7 通过）。

1. **[RULE-1] 时序独立**：Review checkbox 只能在对应文档**完稿之后的独立 pass** 勾选，
   禁止与正文同一次写入。同一次生成的 review 章节必须标注 `[自查·并发]`，
   并在后续独立 pass 复核后升级为 `[已复核]` 或记录发现。
2. **[RULE-2] 统计溯源**：验收统计（CHECKLIST §10.1 类汇总）只能来自**逐项核对表**
   （每行附测试名/实测值证据），禁止从测试通过总数推算分项通过数。
3. **[RULE-3] 环境确定性与测试隔离（v1.2 升级为 quarantine 四要素）**：验收测试不得依赖
   执行环境状态（如"工具链不存在"这类条件 skip）。环境分支用 monkeypatch 强制
   构造，保证任何机器上可复现。确需隔离的测试必须满足四要素（借鉴业界 test
   quarantine pattern，防 skip-and-forget 反模式）：
   - **Owner**：每个隔离测试有明确责任人
   - **Deadline**：隔离超过 30 天必须修复或删除，无例外
   - **降权运行**：隔离 ≠ 跳过——仍每次运行、仍记录结果，只是不阻塞验收结论
   - **Re-qualification**：重新恢复阻塞资格需证明稳定性，单次通过不够
4. **[RULE-4] 单 Agent 自查声明**：同一 agent 既写又审时，review 结论必须标注
   `自查（单视角）`；有条件时应由第二个会话/agent 复审。依据（教训内联）：
   单个 agent 既写又审的结构性缺陷是**无对抗**——审查者与实现者同源，
   继承同一组先入之见与盲区，自查确认偏差（self-confirmation bias）无
   机制制衡；文献佐证：同上下文内"再想一遍"式的反思纠错率 <2%
   （arXiv:2510.08308），LLM 自我纠错盲区率高达 64.5%（arXiv:2507.02778）。
   本规则是流程层的最低成本缓解，根治需规则 5 的异构第二会话。
5. **[RULE-5] 审查异质性约束（v1.2 新增，预注册）**：当引入对抗式/第二会话审查（S1/L1）
   时，reviewer 与 implementer 应使用**异构基座**（不同模型家族）——MAD
   （Multi-Agent Debate）文献（arXiv:2502.08788，ICLR 2025）表明同质 agent
   辩论在 36 个实验场景中对简单 CoT 的胜率无一个超过 20%，却消耗 3-5x token；
   同基座新会话只消除记忆污染、不消除同分布盲区，模型异质性才是增益主源。
   且 reviewer 输出只作标记、**永不直接改写实现**（单向权限）——防
   answer corruption（多智能体文献实证的失效模式：自信但错误的批评
   带偏甚至覆盖本来正确的实现；弱 reviewer 腐蚀强 implementer 的方向
   与"自信的审查者未必正确"同理）。
6. **[RULE-6] 审计证据绑定（v1.3 新增）**：Step 10 的 ADD 审计报告必须包含**取证矩阵**
   标准节——每条审查结论绑定"取证手段 × 覆盖项 × 可重放产物"，受四条子规则
   约束：(a) 证据按可重放性分级 E1-E5（分级表见下文"取证矩阵"节），推测级
   证据（E5）禁止出现在审计结论中；(b) 审计项 ↔ 证据**双向映射**——每个审计
   项至少绑定一条 E1-E4 证据，每条证据标注其覆盖的审计项集合；(c) **诚实
   结果列**——"发现 N 项"（N≥1）是合法且正常的输出，禁止定额化（不得要求
   "必须发现问题"），零发现不直接判合规而触发**降级重扫**（换更高等级证据
   或换角度重扫一次，仍零发现方可记合规并声明扫描范围）；(d) **最高等级
   绑定**——每个审计项绑定其可用的最高可重放等级证据，E1/E2 可得却只绑
   E4 判断陈述视为静默降级（伪造成本回归 O(1)，约束失效）。
   原理（为何有效）：写"✅"成本 O(1) 而复核成本 O(n)，未绑定证据的审查
   结论不可证伪——这是统计溯源规则（规则 2）在审计报告层的推广。当抽查
   概率 p>0 时，形式化审查（未实际执行而打勾）的期望成本 ≥ p×证据重放
   成本；对 E1 级证据，伪造须预生成工件、工件事前成本≈真实执行成本——
   即约束将"假装审查"的最低成本抬升至"真实审查"的成本。
   执行语义警示（Goodhart 防御）：本规则约束**过程与证据形态**，绝不约束
   结论倾向——"全绿"与"发现 N 项"都是合法输出；若被执行为"每行必须✅"，
   规则立即退化为新的形式主义（本项目 M6"并发自查"教训即是无证据绑定的
   全绿表演）。操作模板见下文"取证矩阵（Step 10 审计报告标准节）"。

### Step 2 Review（调研文档幻觉排除）

- [ ] 所有 arXiv 编号已验证（WebSearch 确认）
- [ ] 所有作者、年份、标题已验证
- [ ] 所有技术声明有文献支撑
- [ ] 无未标注的推测
- [ ] 置信度标注完整

**【门禁】（v1.4 新增，[ADR-0008](adr/ADR-0008-spec-process-review-gate-and-bidirectional-check.md) D4）——满足前禁止进入 Step 3**：

- [ ] (a) FALSIFIED 断言已改写并记入修订记录
- [ ] (b) CONFLICT / STEP_GAP_OPEN 已仲裁（状态词表：框架 v1.3+，原单词态读作 OPEN）
- [ ] (c) 阻断性断言双源满足（单源须标 [单源-待二核]）
- [ ] (d) 假设区条目已转 A/B 或以 [待定] 显式携带进下游文档

> **调研异常记录（v1.4，[ADR-0009](adr/ADR-0009-discoveries-log-mechanism.md) D5 集成点）**：调研中遇到数值不稳定、边界 case 异常、工具层失效等，不是"修 bug"，而是**记录发现**至 [docs/discoveries/](docs/discoveries/)（三态索引）——工程问题可能是研究贡献的入口。

### Step 4 Review（设计文档幻觉排除）

- [ ] 设计基于已验证的调研结论
- [ ] 无未经验证的假设
- [ ] 无论证驱动的归因扭曲（教训内联，源自本项目 ADR-0001 案例：为论证某方案的有效性，
      选择性地将一个多因历史案例简化为单因案例——原分析为论证"语义对齐"的重要性，
      把 2008 CDO 失败归因为单一技术原因，扭曲了多层归因的历史事实。检查方法：
      设计文档引用案例/文献支撑其论证时，逐条询问"该案例在原文语境中是否真是
      单因？我是否为了论证强度裁剪了它的复杂性？"——论证需求不得重塑证据形态）
- [ ] 替代方案至少列 2 个并被否决
- [ ] 职责边界清晰（教训内联，源自本项目 ADR-0002 决策：每个设计必须显式声明
      **不覆盖**的问题域，并区分两类边界——**职责外**（该问题有独立的研究范式/
      工具链，本设计不该吞并，声明"不回答"）与**能力边界**（本设计想做但工具
      极限做不到，声明"回答不了"）。混淆两者会诱导后续越界扩张或虚假的能力承诺）

### Step 6 Review（实施文档幻觉排除）

- [ ] 所有依赖版本真实存在（已查询 PyPI/npm/等）
- [ ] **stdlib API 下限检查（v1.1 新增）**：文档中引用的标准库 API 在
      requires-python 下限版本可用（M6 教训：`Decimal.ulp` 为 3.12+，
      在 3.11 运行时 AttributeError）
- [ ] 所有 API 签名可实现
- [ ] 无虚构的库或函数
- [ ] 兼容性声明已验证（含 stdlib，不只三方依赖）
- [ ] 低效操作已排除

### Step 8 Review（文档一致性）

- [ ] 本 review 在**全部四份文档完稿后**执行（v1.1：禁止实现前预填结论）
- [ ] **派生需求登记（v1.2 新增）**：实施中产生、但未在 DESIGN 显式声明的
      设计决策（stub 规则、容差常量、降级策略等）已登记为
      derived-requirement 并单独验收。语义说明（源自 DO-178C §5.5.e，
      适航标准的需求追溯要求，此处内联其义）：派生需求指**不是从上游需求
      推导出来、而是在设计/实施过程中自行产生**的需求——它没有上游追溯链，
      因此是追溯矩阵的结构性盲区；安全关键标准强制要求将其单独标识、
      单独验证，正是为了封堵这个"无上游依据即无人验收"的漏洞
- [ ] RESEARCH.md 的结论在 DESIGN.md 中被使用
- [ ] DESIGN.md 的模块在 IMPLEMENTATION.md 中有对应实施
- [ ] IMPLEMENTATION.md 的功能点在 CHECKLIST.md 中有验收项
- [ ] 无文档间矛盾
- [ ] 术语一致
- [ ] **双向引用完整（v1.4 新增，[ADR-0008](adr/ADR-0008-spec-process-review-gate-and-bidirectional-check.md) D6）**：
      四文档 + 相关 ADR 互引均成立（正向： 文档引用的 ADR 存在；
      反向： 相关 ADR 的"相关文档"含本 feature 链接）——grep 级机械检查
- [ ] **断言延续（v1.4 新增，ADR-0008 D6）**： RESEARCH 的 B 类断言状态在
      DESIGN/IMPLEMENTATION 引用处已按最新词表标注（含 STEP_GAP_CLOSED/OPEN，
      框架 v1.3+）

## 与 ADD（Audit-Driven Development）的关系

Step 10 的"审查验收"执行 ADD 审计。v1.2.1 起 ADD 核心方法内联如下，本文档自包含、不再依赖外部 skill 定义：

### Iron Law（铁律）

**测试通过 ≠ 设计落地。** "164 tests passed"不等于实施完成——测试只能证明代码做了*某件事*，不能证明代码做的是*设计要求的那件事*。这是 ADD 存在的全部理由，也是 Step 10 独立于 Step 9（TDD）的原因。

### 审计四阶段

| 阶段 | 审什么 | 方向 | 典型失效 |
|------|--------|------|---------|
| **Phase 0: Spec 质量门** | DESIGN.md 本身 | —— | 规格模糊（"合理处理""适当时机"）则不可审计，退回重写后再进入代码审计 |
| **Phase 1: 完整性** | DESIGN 每个设计元素在代码中有实现吗 | design → code | 设计了错误处理分支，代码里没有 |
| **Phase 2: 忠实度** | 实现与设计一致吗 | 双向对照 | 静默偏离：实现者自作主张的简化、重排、"等价"改写 |
| **Phase 3: 必要性** | 代码每个行为都有设计依据吗 | code → design | 无设计依据的行为 = 未受控的派生需求（与 Step 8 派生需求登记闭环） |
| **Phase 4: 语义** | 代码字面满足设计条文，但违背设计的解决问题意图吗 | 意图层 | 机械合规但意图错位（原型案例：衍生品定价数学正确但定价的*对象*错了） |

### 产出与修复循环

- 产出结构化审计报告：问题分级（**P1** 阻断验收 / **P2** 应修 / **P3** 提示），每项附代码位置与设计条文引用，附修复优先级矩阵
- 检测 → 修复 → 复审循环，直至 P1 清零
- 审计者永不自动修复：审计输出只标记问题；修复由实现者执行后交复审——与规则 5 的单向权限同构（防自信但错误的批评直接腐蚀实现）
- 审计报告记录到开发日志
- **发现记录（v1.4 新增，[ADR-0009](adr/ADR-0009-discoveries-log-mechanism.md) D5 集成点）**：ADD 审计发现系统性模式（跨 feature / 跨仓库复发的失效模式）时，除标记当代问题外，记录发现至 [docs/discoveries/](docs/discoveries/)（三态索引——学习回路"事故→规则"的载体）

### 取证矩阵（v1.3 新增：Step 10 审计报告标准节，规则 6 的操作形态）

每行一条取证记录，三要素：**取证手段**（含证据等级）、**覆盖项**（审计项编号集合）、**结果**（诚实结果列）。

**证据五分类（按可重放性定级）**：

| 等级 | 类型 | 特性 | 附加约束 |
|------|------|------|---------|
| E1 | 可重放命令（git diff / pytest / --collect-only 输出） | 第三方可原样重放 | 无 |
| E2 | 运行时脚本取证 | 可重跑但**场景自选** | 须附场景选择理由，或包含对抗性场景（边界轮、异常输入），防"场景选择偏差伪装成运行时证据" |
| E3 | 静态读码行号 | 可核对但**随重构腐烂** | 强制绑定 commit hash（`L124@<hash>` 格式） |
| E4 | 盲区扫描 / 判断陈述 | 不可直接重放 | 必须以"发现 N 项"语气报告（零发现须声明扫描范围），不可输出裸✅ |
| E5 | 推测 / 未执行 | 无证据 | **禁止出现在审计结论中**；只能出现在"未覆盖项"清单 |

**矩阵形态示例**：

| 取证手段（等级） | 覆盖项 | 结果 |
|---|---|---|
| pytest 输出（E1） | Q-2/Q-3：文件级测试 + 全量回归 | ✅ 零失败 |
| 运行时脚本取证（E2，含边界轮场景） | D-B5 转换轮 n==K 逐轮验证 | ✅ |
| 静态读码（E3 @commit-hash） | D-A6 校验前置 L79-91 | ✅ |
| git diff 空输出（E1） | D-F1~F5 隔离边界 | ✅ 无越界写入 |
| 盲区扫描（E4，扫描范围：25 项断言恒真式） | 全部断言 | **发现 1 项** |

**风险分级执行**（成本可控，防"全量强制被静默放弃"）：P0 审计项（不变式、隔离边界、安全相关）全量执行且必须绑定 E1/E2 证据；其余审计项抽查执行，被抽中时同样受规则 6 四条子规则约束。

**复核方式**：复审者（规则 4/5 的第二会话或异构基座）抽查时优先抽两类行——"高等级可得却绑低等级"的行（静默降级嫌疑）与 E4 行（不可重放证据的过度依赖）。行号证据按 commit hash 回溯到历史版本核对。

## 与 ADR 的关系

若 spec 流程中产生架构决策，记录为 ADR：
- 选择方案 A 而非 B → ADR
- 框架职责边界调整 → ADR
- 重大技术选型 → ADR

ADR 存放于 `adr/`（编号登记入口: docs/adr/README.md），spec 文档引用相关 ADR。

## 开发日志

每次完成一个 feature 或一个 spec 步骤，记录开发日志到 `docs/dev-log/`：
- `DEV-LOG-XXX-<feature>-<action>.md`
- 记录：做了什么、决策依据、遇到的问题、下一步

## 启动新 feature 的流程

1. 在 `spec/` 下创建 `<feature>/` 目录
2. 复制 4 个模板到该目录
3. 从 Step 1 开始执行 10 步流程
4. 每步完成后更新 `docs/PROGRESS.md`
5. 关键决策记录为 ADR
6. 完成后记录开发日志

## 规则登记（DC4，RULE 命名空间）

```rules
[
  {id: RULE-1, 来源事故: "M6 并发自查（review checkbox 与正文同次 Write，Decimal.ulp 失实声明致 22 测试失败）", 失效条件: "独立 pass 复核被跳过且无第二视角抽查", 拦截记录: []},
  {id: RULE-2, 来源事故: "M6 §10.1 统计虚报（4 项零测试被推算为 7/7 通过）", 失效条件: "逐项核对成本高于全量时被静默放弃", 拦截记录: []},
  {id: RULE-3, 来源事故: "M6 条件 skip（工具链不存在即跳过，环境分支不可复现）", 失效条件: "隔离超 30 天未清理（quarantine Deadline 失守）", 拦截记录: []},
  {id: RULE-4, 来源事故: "M6 元审计（单 agent 既写又审，自查确认偏差无制衡）", 失效条件: "自查声明退化为形式标签且无人抽查", 拦截记录: []},
  {id: RULE-5, 来源事故: "外部对标 MAD 文献（同基座辩论无增益，arXiv:2502.08788）", 失效条件: "同基座新会话被误当作异构审查", 拦截记录: []},
  {id: RULE-6, 来源事故: "M7 审计轮（无证据绑定的全绿表演，ADR-0005）", 失效条件: "Goodhart 退化——被执行为每行必须✅", 拦截记录: []}
]
```

> 拦截记录格式：`{日期, feature, 事故摘要}`——规则真实拦截一次违规时追加一条，作为规则有效性（非僵尸规则）的证据。命名空间权威登记：[ADR-0007 附录 A](adr/ADR-0007-unified-document-contract.md)。
